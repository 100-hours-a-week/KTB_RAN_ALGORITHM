## 🧩 문제

* **문제 제목** : 동전 교환하기
* **문제 레벨** : 강의 문제집
* **문제 유형** : DP (Bottom-up), 최소 개수 최적화
* **문제 제공** : Inflearn Kyo
* **문제 링크** : 강의 문제집 내 제공 문제
* **코드 파일** : [coin-change-kyo.js](../../algorithm-JS/DP/coin-change-kyo.js)

---

## 🧩 문제 설명

동전 종류 배열 `coins`와 목표 금액 `amount`가 주어진다.  
동전은 각 종류를 여러 번 사용할 수 있다.  
`amount`를 만들기 위한 최소 동전 개수를 구하고, 만들 수 없으면 `-1`을 반환한다.

예시) `coins = [1, 3, 4], amount = 6`  
가능한 선택 비교: `1+1+4(3개)`, `3+3(2개)`, `4+1+1(3개)` -> 최소 `2`

---

## 🧩 문제 핵심 포인트

* 이 문제는 "`최소 개수`"를 묻는 전형적인 1차원 DP다.
* `dp[x]`를 "`x`원을 만드는 최소 동전 수"로 정의하면 점화식이 바로 나온다.
* 마지막에 선택한 동전을 `coin`이라고 두면:
  * `x = (x - coin) + coin`
  * `dp[x] = min(dp[x], dp[x - coin] + 1)`
* 도달 불가능 상태를 구분하기 위해 큰 초기값(`amount + 1`)을 사용한다.

---

## 🧩 내 풀이 방식

* `dp`를 길이 `amount + 1`로 만들고 모두 `amount + 1`로 초기화한다.
* `dp[0] = 0`으로 시작한다. (`0`원은 동전 0개)
* `target`을 `1..amount` 순회하며 모든 `coin`을 시도한다.
* `target - coin >= 0`이면 `dp[target]`을 갱신한다.
* 최종적으로 `dp[amount]`가 초기값 그대로면 `-1`, 아니면 그 값을 반환한다.

---

## 🧩 코드 로직 정리

### 문제 유형 판단법

```txt
1. "최소 동전 개수"처럼 최소 비용/최소 개수를 묻는다.
2. 같은 선택(동전)을 여러 번 사용할 수 있다.
3. 목표 금액 target이 더 작은 금액(target - coin) 해답에 의존한다.
=> Bottom-up 1차원 DP로 본다.
```

### 수도코드

```txt
dp[0..amount]를 큰 값으로 초기화
dp[0] = 0

for target = 1..amount:
  for coin in coins:
    if target - coin >= 0:
      dp[target] = min(dp[target], dp[target - coin] + 1)

if dp[amount]가 초기 큰 값이면 -1
else dp[amount] 반환
```

### 전체 흐름

```txt
1. dp 의미 정의: 각 금액별 최소 동전 수
2. 도달 불가 표시용 큰 값으로 초기화
3. target을 1부터 amount까지 순회
4. 각 코인을 붙여보며 최소값 갱신
5. amount 결과를 -1 규칙으로 반환
```

---

## 🧩 코드 구현 (내 풀이)

```js
function solution(coins, amount) {
  //금액 X
  //dp[x] = x를 만들기 위해 필요한 최소 동전의 개수
  //마지막에 선택한 동전 coin
  //x = (x-coin) + coin(이번에 선택한 코인)

  //dp[x] = Math.min(dp[x], dp[x-coin] + 1)

  //[1,3,4] => 6을 만들기
  //dp[6] = dp[5] + 1 (coin=1)
  //dp[6] = dp[3] + 1 (coin=3)
  //dp[6] = dp[2] + 1 (coin=4)

  // dp[x] = 금액 x를 만들기 위한 최소 동전 개수
  const dp = Array(amount + 1).fill(amount + 1);
  dp[0] = 0;

  for (let target = 1; target <= amount; target++) {
    for (const coin of coins) {
      if (target - coin >= 0) {
        dp[target] = Math.min(dp[target], dp[target - coin] + 1);
      }
    }
  }

  //끝까지 초기값이면 amount를 만들 수 없는 경우
  return dp[amount] === amount + 1 ? -1 : dp[amount];
}
```

---

## 🧩 사용된 JS 개념

* `Array(amount + 1).fill(...)` : DP 테이블 초기화
* `for...of` : 동전 종류 순회
* `Math.min()` : 최소 개수 상태 갱신

---

## 🧩 복잡도

* 시간복잡도 : `O(amount * coins.length)`
* 공간복잡도 : `O(amount)`

---

## 🧠 사고 키워드

* 최소 개수 DP
* 부분 문제 재사용
* 도달 불가능 상태 처리
* Bottom-up

---

## 🔍 트리거 문장

* "금액을 만들기 위한 최소 동전 개수"
* "동전은 여러 번 사용 가능"
* "만들 수 없으면 -1"

---

## ⚠️ 오답 포인트

* `for (target < amount)`로 돌리면 `amount` 자체가 계산되지 않는다.
* `return dp`(배열 전체 반환)로 두면 정답 형식이 깨진다.
* 초기값을 `0`이나 작은 값으로 두면 도달 불가능 상태 판별이 안 된다.
