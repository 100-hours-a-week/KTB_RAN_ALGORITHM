## 🧩 문제

* **문제 제목** : 계단 오르기
* **문제 레벨** : 실버 3
* **문제 유형** : DP (Bottom-up), 선택/비선택
* **문제 제공** : BOJ 2579
* **문제 링크** : https://www.acmicpc.net/problem/2579
* **코드 파일** : [stair-score-2579.js](../../algorithm-JS/DP/stair-score-2579.js)

---

## 🧩 문제 설명

각 계단에 점수가 있고, 마지막 계단은 반드시 밟아야 한다.  
한 번에 1칸 또는 2칸 이동할 수 있지만, 연속된 3개 계단을 모두 밟을 수는 없다.  
얻을 수 있는 총 점수의 최댓값을 구한다.

---

## 🧩 문제 핵심 포인트

* 마지막 계단 `i`는 반드시 밟아야 하므로 `dp[i]`를 "`i`를 밟고 도착한 최대 점수"로 둔다.
* `i`에 도달하는 방법은 2가지:
  * `i-2 -> i` (한 칸 건너뛰기)
  * `i-3 -> i-1 -> i` (연속 2칸 밟기)
* 점화식:
  * `dp[i] = Math.max(dp[i-2], dp[i-3] + stair[i-1]) + stair[i]`

---

## 🧩 내 풀이 방식

* 계산을 단순하게 하려고 계단 점수 배열을 1-based로 변환했다.
* 기저값:
  * `dp[1] = stair[1]`
  * `dp[2] = stair[1] + stair[2]`
  * `dp[3] = max(stair[1]+stair[3], stair[2]+stair[3])`
* `i=4`부터 점화식으로 채우고 `dp[n]`을 반환한다.

---

## 🧩 코드 로직 정리

### 전체 흐름

```txt
1. n=1,2 예외 처리
2. stair를 1-based 배열로 변환
3. dp[1], dp[2], dp[3] 기저값 설정
4. i=4..n 순회
5. dp[i] = max(dp[i-2], dp[i-3] + stair[i-1]) + stair[i]
6. dp[n] 반환
```

---

## 🧩 코드 구현 (내 풀이)

```js
//계단오르기 문제
// 계단 오르는 데는 다음과 같은 규칙이 있다.
// 1) 계단은 한 번에 한 계단씩 또는 두 계단씩 오를 수 있다.
// 2) 연속된 세 개의 계단을 모두 밟아서는 안 된다.
// 3) 마지막 도착 계단은 반드시 밟아야 한다.
//
// d[i] = i번째 계단을 "반드시 밟고" 도달했을 때 얻을 수 있는 최대 점수
// 점화식(1-based):
// d[i] = Math.max(d[i-2], d[i-3] + stair[i-1]) + stair[i]

const fs = require("fs"); // /dev/stdin
const input = fs.readFileSync("../예제.txt").toString().trim().split("\n");

//백준 제출 시
// const fs = require("fs");
// const input = fs.readFileSync(0, "utf-8").trim().split("\n");

function solution(n, arr) {
  if (n === 1) return arr[0];
  if (n === 2) return arr[0] + arr[1];

  // 1-based 인덱스로 변환
  const stair = [0, ...arr];
  const dp = Array(n + 1).fill(0);

  dp[1] = stair[1];
  dp[2] = stair[1] + stair[2];
  dp[3] = Math.max(stair[1] + stair[3], stair[2] + stair[3]);

  for (let i = 4; i <= n; i++) {
    dp[i] = Math.max(dp[i - 2], dp[i - 3] + stair[i - 1]) + stair[i];
  }

  return dp[n];
}

const n = Number(input[0]);
const arr = input.slice(1).map(Number);

console.log(solution(n, arr));
```

---

## 🧩 사용된 JS 개념

* 전개 연산자(`...`) : 1-based 배열 생성
* `Array(n + 1).fill(0)` : DP 배열 초기화
* `Math.max()` : 두 경로 중 최댓값 선택

---

## 🧩 복잡도

* 시간복잡도 : `O(n)`
* 공간복잡도 : `O(n)`

---

## 🧠 사고 키워드

* 마지막 칸 필수
* 연속 3칸 금지
* 2개 경로 분기 DP

---

## 🔍 트리거 문장

* "마지막 계단은 반드시 밟아야 한다"
* "연속된 세 개의 계단 금지"
* "최댓값"

---

## ⚠️ 오답 포인트

* `dp[i-1]`을 그대로 쓰면 마지막 칸을 밟지 않는 상태가 섞여 틀릴 수 있다.
* `dp[3]` 기저값을 잘못 두면 이후가 전부 어긋난다.
* 인덱스를 0-based/1-based 혼용하면 점화식이 꼬인다.

---

## 📝 회고 KPT

### 🟢 Keep

* DP 문제에서 점화식을 바로 쓰기보다, **dp[i]의 의미를 정의하려고 시도했다.**
* 단순 피보나치식 접근이 맞지 않다는 것을 테스트 케이스로 확인했다.
* 반복적으로 점화식을 수정하며 “연속 3칸 금지” 조건을 반영하려고 했다.
* 바텀업으로 구현하며 상태 전이를 직접 검증했다.

### 🔴 Problem

* 초기 상태 정의가 불충분했다.
  → `dp[i] = i번째까지 최대 점수`로만 정의해, **연속 상태 정보가 부족했다.**
* `dp[i-1]`을 그대로 사용하는 점화식을 세웠는데,
  → 이 경우 “연속 3칸” 여부를 구분할 수 없다는 점을 간과했다.
* 조건을 단순히 “수식에 더하는 방식”으로 처리하려는 경향이 있었다.
  → 실제로는 가능한 경로를 나열한 뒤, 합법 케이스만 추려야 했다.
* 점화식 설계 전에
  “i에 도달하는 모든 합법 경로는 무엇인가?”를 충분히 분해하지 않았다.

### 🟡 Try

* 앞으로 DP 문제는 다음 순서로 접근한다:
  1. **dp[i]를 한 문장으로 명확히 정의한다.**
  2. i에 도달하는 “모든 가능한 직전 상태”를 나열한다.
  3. 문제의 제약 조건(연속 제한 등)이 어떤 상태를 제거하는지 먼저 확인한다.
  4. 남은 합법 상태만 점화식에 포함한다.
* `dp[i-1]`을 사용할 때는 그 값이 “어떤 상태를 포함하는지” 먼저 검증한다.
* 제약이 있는 DP 문제는 단순 최대/최소 비교가 아니라 **상태 전이 구조를 먼저 설계**한다.
