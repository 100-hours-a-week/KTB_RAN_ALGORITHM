## 🧩 문제

* **문제 제목** : 도둑의 최적 전략
* **문제 레벨** : 강의 문제집
* **문제 유형** : DP (Bottom-up), 선택/비선택
* **문제 제공** : Inflearn Kyo
* **문제 링크** : 강의 문제집 내 제공 문제
* **코드 파일** : [house-robber-kyo.js](../../algorithm-JS/DP/house-robber-kyo.js)

---

## 🧩 문제 설명

일렬로 배치된 집마다 훔칠 수 있는 금액 `money[i]`가 주어진다.  
단, 인접한 두 집은 동시에 털 수 없다.  
이 제약을 지키면서 훔칠 수 있는 최대 금액을 구한다.

예시) `money = [2, 7, 9, 3, 1]` -> 정답 `12` (`2 + 9 + 1`)

---

## 🧩 문제 핵심 포인트

* 매 집에서 선택지는 2개뿐이다: `털기` or `안 털기`.
* `i`번째 집을 털면 바로 이전 집(`i-1`)은 못 턴다.
* 그래서 `dp[i]`는 아래 둘 중 최대다.
  * `dp[i-1]` : i를 털지 않음
  * `dp[i-2] + money[i]` : i를 털고 i-2까지의 최적해를 더함
* 점화식: `dp[i] = Math.max(dp[i-1], dp[i-2] + money[i])`

---

## 🧩 내 풀이 방식

* 상태를 `dp[i] = 0..i번째 집까지 고려했을 때 최대 금액`으로 정의한다.
* 기저값을 먼저 고정한다.
  * `dp[0] = money[0]`
  * `dp[1] = Math.max(money[0], money[1])`
* `i=2`부터 `n-1`까지 점화식으로 누적 계산한다.
* 마지막 값 `dp[n-1]`이 정답이다.

---

## 🧩 코드 로직 정리

### 문제 유형 판단법

```txt
1. 인접 원소를 동시에 선택할 수 없다는 제약이 있다.
2. "최대 합"을 묻는다.
3. 현재 선택이 이전 상태(i-1, i-2)에만 의존한다.
=> 선택/비선택 DP(Bottom-up)로 본다.
```

### 수도코드

```txt
n = money.length
if n == 0: return 0
if n == 1: return money[0]

dp[0] = money[0]
dp[1] = max(money[0], money[1])

for i = 2..n-1:
  skip = dp[i-1]
  take = dp[i-2] + money[i]
  dp[i] = max(skip, take)

return dp[n-1]
```

### 전체 흐름

```txt
1. 빈 배열/원소 1개 예외 처리
2. dp 의미를 "i까지의 최대 금액"으로 정의
3. dp[0], dp[1] 기저값 설정
4. i=2부터 털기/안털기 비교 갱신
5. dp[n-1] 반환
```

---

## 🧩 코드 구현 (내 풀이)

```js
function solution(money) {
  //도둑의 최적 전략
  //점화식을 만드는 방법
  //dp[i] => i번째 집까지 생각했을 때, 도둑이 훔칠 수 있는 최대 금액

  //1. i번째 집을 털지 않는다.
  //-> dp[i-1]
  //2. i번째 집을 턴다.
  //연속된 두 집을 동시에 털 수 없기 때문
  //-> dp[i-2] + money[i]

  //점화식
  //dp[i] = Math.max(dp[i-1], dp[i-2] + money[i])

  const n = money.length;
  if (n === 0) return 0;
  if (n === 1) return money[0];

  //dp[i] = 0..i번째 집까지 고려했을 때 훔칠 수 있는 최대 금액
  const dp = Array(n).fill(0);

  //기저값
  dp[0] = money[0];
  dp[1] = Math.max(money[0], money[1]);

  for (let i = 2; i < n; i++) {
    //i번째 집을 털지 않는 경우 vs 터는 경우
    dp[i] = Math.max(dp[i - 1], dp[i - 2] + money[i]);
  }

  return dp[n - 1];
}
```

---

## 🧩 사용된 JS 개념

* `Array(n).fill(0)` : DP 배열 초기화
* `Math.max()` : 두 선택지(털기/안 털기) 중 최대 선택
* 경계값 처리 `if` : `n=0`, `n=1` 예외 처리

---

## 🧩 복잡도

* 시간복잡도 : `O(n)`
* 공간복잡도 : `O(n)`

---

## 🧠 사고 키워드

* 선택/비선택
* 인접 선택 금지
* 점화식
* Bottom-up DP

---

## 🔍 트리거 문장

* "인접한 두 집은 동시에 선택 불가"
* "최대 금액/최대 합"
* "각 칸에서 털까 말까 결정"

---

## ⚠️ 오답 포인트

* `if (n == 0)`를 두 번 쓰는 실수: 하나는 `n == 1`이어야 한다.
* `return dp`로 배열 전체를 반환하면 오답이다. `dp[n-1]`을 반환해야 한다.
* 기저값 없이 반복문부터 돌리면 `n=1`, `n=2`에서 깨진다.
