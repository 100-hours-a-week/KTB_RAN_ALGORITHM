## 🧩 문제

* **문제 제목** : 시식투어 챌린지
* **문제 레벨** : 강의 문제집
* **문제 유형** : DP (Bottom-up), 선택/비선택, 연속 선택 제한
* **문제 제공** : Inflearn Kyo
* **문제 링크** : 강의 문제집 내 제공 문제
* **코드 파일** : [tasting-tour-challenge-kyo.js](../../algorithm-JS/DP/tasting-tour-challenge-kyo.js)

---

## 🧩 문제 설명

시식 부스마다 먹을 수 있는 고기 양 `meats[i]`가 주어진다.  
단, 연속된 3개 부스를 모두 먹을 수는 없다.  
이 제약을 지키면서 먹을 수 있는 고기 양의 최댓값을 구한다.

---

## 🧩 문제 핵심 포인트

* 제약은 "연속 3개 선택 금지"다.
* `i`번째 부스를 기준으로 보면 선택 경우의 수는 3가지로 고정된다.
  * `i`를 안 먹는다 -> `dp[i-1]`
  * `i`만 먹고 `i-1`은 안 먹는다 -> `dp[i-2] + meats[i]`
  * `i-1`, `i`를 먹고 `i-2`는 안 먹는다 -> `dp[i-3] + meats[i-1] + meats[i]`
* 점화식:
  * `dp[i] = max(dp[i-1], dp[i-2] + meats[i], dp[i-3] + meats[i-1] + meats[i])`

---

## 🧩 내 풀이 방식

* 상태를 `dp[i] = 0..i번째 부스까지 고려했을 때 최대 고기 양`으로 정의한다.
* 작은 입력(`n=0,1,2`)은 바로 처리한다.
* 기저값:
  * `dp[0] = meats[0]`
  * `dp[1] = meats[0] + meats[1]`
  * `dp[2] = max(dp[1], meats[0]+meats[2], meats[1]+meats[2])`
* `i=3`부터 점화식으로 누적 계산하고 `dp[n-1]`을 반환한다.

---

## 🧩 코드 로직 정리

### 문제 유형 판단법

```txt
1. "최대 합"을 묻는다.
2. 인접 제약(연속 3개 금지)이 있다.
3. 현재 선택이 i-1, i-2, i-3 상태에 의존한다.
=> Bottom-up DP로 본다.
```

### 수도코드

```txt
n = meats.length
if n == 0: return 0
if n == 1: return meats[0]
if n == 2: return meats[0] + meats[1]

dp[0] = meats[0]
dp[1] = meats[0] + meats[1]
dp[2] = max(dp[1], meats[0] + meats[2], meats[1] + meats[2])

for i = 3..n-1:
  case1 = dp[i-1]
  case2 = dp[i-2] + meats[i]
  case3 = dp[i-3] + meats[i-1] + meats[i]
  dp[i] = max(case1, case2, case3)

return dp[n-1]
```

### 전체 흐름

```txt
1. dp[i] 의미 정의
2. 경계값(n=0,1,2) 처리
3. dp[0], dp[1], dp[2] 기저값 세팅
4. i=3부터 3가지 케이스 최대값으로 갱신
5. dp[n-1] 반환
```

---

## 🧩 코드 구현 (내 풀이)

```js
function solution(meats) {
  //dp[i] => i번째 부스까지 고려했을 때 먹을 수 있는 최대 고기의 양

  //i번째 부스까지 고려할 때, 3가지 경우
  //1. i번째 부스를 먹지 않는 경우
  //-> dp[i-1]
  //2. i번째 부스를 먹고, i-1번째 부스는 먹지 않는 경우
  //-> dp[i-2] + meats[i]
  //3. i번째 부스를 먹고, i-1번째 부스를 먹고, i-2번째 부스는 먹지 않는 경우
  //-> dp[i-3] + meats[i-1] + meats[i]

  //dp[i] = Math.max(dp[i-1], dp[i-2]+meats[i], dp[i-3]+meats[i-1]+meats[i])

  const n = meats.length;

  if (n === 0) return 0;
  if (n === 1) return meats[0];
  if (n === 2) return meats[0] + meats[1];

  //dp[i] = 0..i번째 부스까지 고려했을 때 먹을 수 있는 최대 고기 양
  const dp = Array(n).fill(0);

  dp[0] = meats[0];
  dp[1] = meats[0] + meats[1];
  dp[2] = Math.max(dp[1], meats[0] + meats[2], meats[1] + meats[2]);

  for (let i = 3; i < n; i++) {
    //연속 3개를 먹지 않도록 3개 케이스를 비교
    dp[i] = Math.max(
      dp[i - 1],
      dp[i - 2] + meats[i],
      dp[i - 3] + meats[i - 1] + meats[i]
    );
  }

  return dp[n - 1];
}
```

---

## 🧩 사용된 JS 개념

* `Array(n).fill(0)` : DP 배열 생성
* `Math.max()` : 3가지 선택지 중 최댓값 갱신
* 경계값 처리 `if` : 작은 입력 안전 처리

---

## 🧩 복잡도

* 시간복잡도 : `O(n)`
* 공간복잡도 : `O(n)`

---

## 🧠 사고 키워드

* 연속 선택 제한 DP
* 케이스 분기 3개
* 상태 전이
* Bottom-up

---

## 🔍 트리거 문장

* "연속 3개는 불가능"
* "최대로 먹을 수 있는 양"
* "현재 칸을 고를지 말지"

---

## ⚠️ 오답 포인트

* `dp[2]` 계산에서 `dp[1]`을 후보에 넣지 않으면 오답이 난다.
* 마지막에 `return dp`(배열 전체)를 반환하면 정답 형식이 깨진다.
* `n=0,1,2` 경계값 처리를 빼면 인덱스 오류가 난다.
