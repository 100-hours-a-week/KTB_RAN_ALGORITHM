## 🧩 문제

* **문제 제목** : 1로 만들기
* **문제 레벨** : 실버 3
* **문제 유형** : DP (Bottom-up), 최소 연산 횟수
* **문제 제공** : BOJ 1463
* **문제 링크** : https://www.acmicpc.net/problem/1463
* **코드 파일** : [make-one-1463.js](../../algorithm-JS/DP/make-one-1463.js)

---

## 🧩 문제 설명

정수 `X`가 주어질 때, 아래 연산만 사용해서 `1`을 만들려고 한다.

* `X`가 3으로 나누어떨어지면 `X / 3`
* `X`가 2로 나누어떨어지면 `X / 2`
* `X - 1`

필요한 연산 횟수의 최솟값을 구한다.

---

## 🧩 문제 핵심 포인트

* `i`를 1로 만드는 최소 횟수는 더 작은 수들의 최소 횟수로 구성된다.
* 현재 `i`에서 가능한 이전 상태는 최대 3개다: `i-1`, `i/2`, `i/3`.
* 점화식:
  * `dp[i] = min(dp[i-1] + 1, dp[i/2] + 1(가능 시), dp[i/3] + 1(가능 시))`

---

## 🧩 내 풀이 방식

* `dp[i]`를 "`i`를 1로 만드는 최소 연산 횟수"로 정의한다.
* `dp[1] = 0`부터 시작하고, `2..n`까지 순서대로 채운다.
* 매 `i`마다 3가지 연산 후보를 비교해 최소값을 저장한다.
* 마지막 `dp[n]`을 반환한다.

---

## 🧩 코드 로직 정리

### 문제 유형 판단법

```txt
1. "최소 연산 횟수"를 묻는다.
2. 현재 값 i의 답이 더 작은 값(i-1, i/2, i/3)의 답에 의존한다.
3. 같은 계산이 반복된다.
=> Bottom-up DP로 해결한다.
```

### 수도코드

```txt
dp[0..n] 초기화
dp[1] = 0

for i = 2..n:
  a = dp[i-1] + 1
  b = (i % 2 == 0) ? dp[i/2] + 1 : INF
  c = (i % 3 == 0) ? dp[i/3] + 1 : INF
  dp[i] = min(a, b, c)

return dp[n]
```

### 전체 흐름

```txt
1. dp 의미 정의
2. 기저값 dp[1] 설정
3. i=2..n 순회
4. 3가지 연산 후보의 최소값 저장
5. dp[n] 반환
```

---

## 🧩 코드 구현 (내 풀이)

```js
//1. dp 정의
// dp[i] = i를 1로 만드는 연산 사용횟수 최솟값

//2. i번째에 내가 할수있는 선택.
// 1. X가 3으로 나누어 떨어지면 3을 나눈다.
// 2. X가 2로 나누어 떨어지면 2를 나눈다.
// 3. X에서 -1를 뺀다

//4 -> %2 %2 | -1 %3
//d[0] return 0
//d[4] = d[3] + 1(-1) | d[i-1] + 1
//d[2] = d[2] + 1(%2) | d[i%2] + 1
//3으로 못나눔 | d[i%3] + 1

//dp[i] = Math.min(i%2?d[i%2]+1, i%3?d[i%3]+1, d[i-1]+1);

const fs = require("fs"); // /dev/stdin
const input = fs.readFileSync("../예제.txt").toString().trim().split("\n");

//백준 제출 시
// const fs = require("fs");
// const input = fs.readFileSync(0, "utf-8").trim().split("\n");

function solution(n) {
  const dp = Array(n + 1).fill(n);

  if (n == 0) return 0;
  if (n == 1) return 0;

  dp[0] = 0;
  dp[1] = dp[0];

  for (let i = 2; i <= n; i++) {
    dp[i] = Math.min(
      i % 2 === 0 ? dp[i / 2] + 1 : n,
      i % 3 === 0 ? dp[i / 3] + 1 : n,
      dp[i - 1] + 1
    );
  }

  return dp[n];
}

const n = Number(input[0]);

console.log(solution(n));
```

---

## 🧩 사용된 JS 개념

* `Array(n + 1).fill(...)` : DP 배열 초기화
* `Math.min()` : 최소 연산 횟수 선택
* 삼항 연산자 : 조건부 후보 처리 (`/2`, `/3`)

---

## 🧩 복잡도

* 시간복잡도 : `O(n)`
* 공간복잡도 : `O(n)`

---

## 🧠 사고 키워드

* 최소 연산 DP
* 상태 전이
* bottom-up
* 부분 문제 최적해

---

## 🔍 트리거 문장

* "1로 만들기"
* "최소 횟수"
* "`/3`, `/2`, `-1` 연산"

---

## ⚠️ 오답 포인트

* `n=1` 같은 작은 값의 기저값 처리를 빼먹으면 오답이 난다.
* `i/2`, `i/3`는 나누어떨어질 때만 후보로 써야 한다.
* `dp` 전체 배열을 반환하지 않고 반드시 `dp[n]`을 반환해야 한다.

