## 🧩 문제

* **문제 제목** : 계단오르기
* **문제 레벨** : 강의 문제집
* **문제 유형** : DP, 점화식
* **문제 제공** : Inflearn Kyo
* **문제 링크** : 강의 문제집 내 제공 문제
* **코드 파일** : [stair-climbing-kyo.js](../../algorithm-JS/DP/stair-climbing-kyo.js)

---

## 🧩 문제 설명

한 번에 `1칸` 또는 `2칸`만 오를 수 있을 때,  
`n`칸 계단에 도달하는 서로 다른 경우의 수를 구한다.

예시) `n = 4`  
`1+1+1+1`, `1+1+2`, `1+2+1`, `2+1+1`, `2+2` -> 총 `5`가지

---

## 🧩 문제 핵심 포인트

* `i`번째 칸에 도착하는 마지막 이동은 `1칸` 또는 `2칸`뿐이다.
* 따라서 `dp[i] = dp[i-1] + dp[i-2]` 점화식을 세울 수 있다.
* 작은 문제(`dp[1]`, `dp[2]`)를 먼저 채우고 큰 문제로 확장한다.

---

## 🧩 내 풀이 방식

* `dp[i]`를 "`i`칸까지 가는 방법 수"로 정의한다.
* 기저값을 `dp[1]=1`, `dp[2]=2`로 설정한다.
* `i=3`부터 `n`까지 점화식으로 누적 계산한다.
* 마지막에 `dp[n]`을 반환한다.

---

## 🧩 코드 로직 정리

### 전체 흐름

```txt
1. n이 1 또는 2면 기저값 즉시 반환
2. dp 배열 생성 후 dp[1], dp[2] 초기화
3. i=3..n 순회
4. dp[i] = dp[i-1] + dp[i-2]
5. dp[n] 반환
```

---

## 🧩 코드 구현 (내 풀이)

```js
function solution(n) {
  //n = 4
  //1 1 1 1 | 1 1 2 | 1 2 1 | 2 1 1 | 2 2
  //계단 오르기 서로 다른 경우의 수

  //n개까지 가는 방법을 알기 위해
  //n-1에서 한 칸 오르기
  //n-2에서 두 칸 오르기

  //1. 점화식을 작성하기
  //dp[i] = i번째 계단까지 올라가는 방법의 수
  //dp[i] = dp[i-1] + dp[i-2]

  //2. 작은 문제를 풀어놓기
  //dp[1] = 1
  //dp[2] = 2
  //2층 계단을 오를 때 [1+1], [2] -> 2가지

  //Bottom-up
  if (n === 1) return 1;
  if (n === 2) return 2;

  const dp = Array(n + 1).fill(0);
  dp[1] = 1;
  dp[2] = 2;

  for (let i = 3; i <= n; i++) {
    dp[i] = dp[i - 1] + dp[i - 2];
  }

  return dp[n];
}
```

---

## 🧩 사용된 JS 개념

* `Array(n + 1).fill(0)` : DP 테이블 초기화
* `for` 반복문 : bottom-up 점화식 계산
* 조기 반환(`if`) : 기저값 처리

---

## 🧩 복잡도

* 시간복잡도 : `O(n)`
* 공간복잡도 : `O(n)`

---

## 🧠 사고 키워드

* 경우의 수 DP
* 점화식
* 기저값
* bottom-up

---

## 🔍 트리거 문장

* "1칸 또는 2칸 이동"
* "n칸까지 가는 방법 수"
* "서로 다른 경우의 수"

---

## ⚠️ 오답 포인트

* `dp[n]` 반환을 빼먹으면 `undefined`가 나온다.
* `dp[0]`, `dp[1]` 등 인덱스 기준을 혼동하면 점화식이 어긋난다.
* 작은 `n`(1, 2) 처리 없이 반복문만 쓰면 경계값에서 틀릴 수 있다.
