## 🧩 문제

* **문제 제목** : 다리를 지나는 트럭
* **문제 레벨** : Level 2
* **문제 유형** : 스택/큐 (Queue)
* **문제 제공** : Programmers
* **문제 링크** : https://school.programmers.co.kr/learn/courses/30/lessons/42583
* **코드 파일** : [process.js](../../algorithm-JS/1.%20Stack_Queue_Hash/process.js)
* **추가 코드 파일** : [bridge-42843.js](../../algorithm-JS/1.%20Stack_Queue_Hash/bridge-42843.js)

---

## 🧩 문제 핵심 포인트

* 트럭은 입력된 순서를 유지해서 다리에 올라가야 함
* 다리 위 트럭 수는 `bridge_length`를 넘을 수 없음
* 다리 위 트럭 무게 합은 `weight`를 넘을 수 없음
* 매 초마다
  * 다리 위 트럭 이동 시간 증가
  * 맨 앞 트럭이 다리를 다 건넜는지 확인
  * 새 트럭 진입 가능 여부 확인

---

## 🧩 내 주석 모아서 정리한 풀이 방식

코드 주석 기준으로 내가 푼 흐름은 아래와 같음.

* 트럭 여러 대가 일차선 다리를 정해진 순서대로 건너고, 모든 트럭이 다 건너는 최소 시간을 구한다.
* 제한 조건은 2가지로 본다.
* 다리 위 트럭 수는 `bridge_length` 이하여야 한다.
* 다리 위 트럭 무게 합은 `weight` 이하여야 한다.
* 자료구조는 큐 방식으로 잡는다.
* `truck_weights`는 대기 트럭 큐, `bridge`는 현재 다리 위 트럭 큐로 사용한다.
* `truck_success` 배열로 다리를 지난 트럭을 모아서 종료 조건(`성공 트럭 수 === 전체 트럭 수`)으로 쓴다.
* `current_time` 배열로 다리 위 각 트럭의 경과 시간을 관리한다.
* 매 반복마다 시간을 1초 증가시키고, 다리 위 트럭들의 경과 시간도 전부 +1 한다.
* 맨 앞 트럭의 경과 시간이 `bridge_length`를 넘으면 다리를 완전히 건넌 것으로 보고 `bridge`에서 제거한다.
* 대기열 맨 앞 트럭을 꺼내서 현재 다리 무게 합 + 해당 트럭 무게가 `weight` 이하인지 확인한다.
* 가능하면 다리에 올리고, 불가능하면 다시 대기열 앞(`unshift`)으로 돌려보낸다.
* 위 과정을 반복해서 최종 `time`을 반환한다.

---

## 🧩 코드 로직 정리

### 전체 흐름

```txt
1. time을 1초씩 증가
2. 다리 위 트럭들의 경과 시간 +1
3. 맨 앞 트럭이 bridge_length를 초과하면 다리에서 제거
4. 다음 트럭이 무게 제한 내에서 진입 가능하면 진입
5. 불가능하면 대기열 맨 앞으로 복귀
6. 모든 트럭이 다리를 건널 때까지 반복
```

---

## 🧩 코드 구현 (내 풀이)

```js
function solution(bridge_length, weight, truck_weights) {

    let truck_success = [];
    
    let len = truck_weights.length;
    let time = 0;
    
    let bridge = [];
    let current_truck = 0;
    
    
    let current_time = [];

    let truck_order = [...truck_weights];
    
    let head_time = 0;
    
    let bridge_first = 0;

    while (truck_success.length != len) {
        time++;
        current_truck = truck_weights.shift();
        
        current_time = current_time.map(i => i + 1);
        head_time = current_time[0];

        if (head_time > bridge_length) {
            truck_success.push(bridge.shift());
            current_time.shift();
        }
        
        if ((bridge.length == 0) || (bridge.reduce((a, b) => a + b) + current_truck <= weight)) {
            bridge.push(current_truck);
            current_time.push(1);
        } else {
            truck_weights.unshift(current_truck);
        }
    }
    
    return time;
}
```

---

## 🧩 사용된 JS 개념

* `shift()` : 대기열/다리 맨 앞 요소 추출
* `unshift()` : 조건 불만족 트럭을 다시 대기열 앞에 삽입
* `reduce()` : 다리 위 트럭 무게 합 계산
* `map()` : 다리 위 각 트럭의 경과 시간 증가 처리

---

## 🧩 코드 리뷰 기반 개선 정리

### 기존 풀이의 장점

* 시뮬레이션 흐름이 문제 요구사항과 정확히 일치한다.
* `time` 증가, 다리 위 경과 시간 추적, 다리 이탈 조건이 명확하다.
* 입력 순서를 유지하며 트럭을 처리해 정답 기준을 충족한다.

### 아쉬운 점과 개선 근거

* `shift()`/`unshift()`를 반복 사용하면 배열 재정렬이 발생해 비용이 커진다.
* `reduce()`로 다리 무게 합을 매 반복마다 다시 계산하면 누적 비용이 증가한다.
* `bridge`와 `current_time`을 분리 관리하면 인덱스 동기화 부담이 생긴다.

### 개선 핵심 아이디어

* 다리를 `bridge_length` 고정 길이 큐로 두고 매 초마다 한 칸씩 이동시킨다.
* 다리 칸의 값은 `0`(빈칸) 또는 `트럭 무게`로 표현한다.
* 다리 총 무게는 `bridgeWeight` 변수로 누적 관리한다.

### 개선 풀이 플로우 요약

```txt
1. 길이가 bridge_length인 다리 큐를 0으로 초기화
2. 매 초(time++)마다 다리 맨 앞을 빼서(이탈) bridgeWeight에서 차감
3. 다음 트럭이 올라갈 수 있으면 push(트럭), bridgeWeight에 더함
4. 못 올라가면 push(0)으로 빈칸 추가
5. 대기 트럭이 없고 bridgeWeight도 0이 되면 종료
6. 누적된 time 반환
```

### 개선 코드 예시

```js
function solution(bridge_length, weight, truck_weights) {
  const bridge = Array(bridge_length).fill(0);
  let bridgeWeight = 0;
  let time = 0;

  while (truck_weights.length > 0 || bridgeWeight > 0) {
    time++;

    bridgeWeight -= bridge.shift();

    if (truck_weights.length > 0 && bridgeWeight + truck_weights[0] <= weight) {
      const truck = truck_weights.shift();
      bridge.push(truck);
      bridgeWeight += truck;
    } else {
      bridge.push(0);
    }
  }

  return time;
}
```

### 기존 vs 개선 비교

| 항목 | 기존 풀이 | 개선 풀이 |
| --- | --- | --- |
| 시간 비용 | 높아질 수 있음 | 더 안정적 |
| 상태 관리 | 배열 3개 | 배열 1개 + 합계 변수 |
| 가독성 | 중간 | 높음 |
| 실수 가능성 | 상대적으로 높음 | 낮음 |
