## 🧩 문제

* **문제 제목** : 수 고르기
* **문제 레벨** : Gold 5
* **문제 유형** : 투 포인터 (Two Pointers), 정렬
* **문제 제공** : BOJ
* **문제 링크** : https://www.acmicpc.net/problem/2230
* **코드 파일** : [select-number-2230.js](../../algorithm-JS/1.%20Stack_Queue_Hash/select-number-2230.js)

---

## 🧠 문제 키워드

* 사용 알고리즘 : 정렬 + 투 포인터
* 차이(`arr[right] - arr[left]`)가 `M` 이상인 최소값 찾기
* `diff < M`이면 오른쪽 확장
* `diff >= M`이면 답 갱신 후 왼쪽 축소
* 포인터 불변식(`left < right`) 유지

---

## 🧩 문제 설명

정수 `N`개 중 두 수를 골라 차이가 `M` 이상이 되도록 할 때,
그 차이의 최솟값을 구하는 문제다.

---

## 🧩 제한사항

* `1 ≤ N ≤ 100,000`
* `0 ≤ M ≤ 2,000,000,000`

---

## 🧩 입출력 예

| 입력 | 출력 |
| --- | --- |
| 3 3 / 1 / 5 / 3 | 4 |

---

## 🧩 문제 핵심 포인트

* 정렬 후 `right`를 늘리면 차이는 커지고, `left`를 늘리면 차이는 작아진다.
* 이 단조성 덕분에 투 포인터로 `O(n)` 스캔이 가능하다.
* 전체 시간복잡도는 정렬 포함 `O(n log n)`.

---

## 🧩 내 풀이 방식

* 배열을 오름차순 정렬한다.
* `left`, `right` 두 포인터로 현재 차이 `diff`를 계산한다.
* `diff < M`이면 조건을 만족하기 위해 `right`를 증가.
* `diff >= M`이면 후보 답을 갱신하고 더 작은 차이를 찾기 위해 `left`를 증가.
* `left`와 `right`가 겹치면 의미 없는 비교를 막기 위해 `right++`.

---

## 🧩 코드 로직 정리

### 전체 흐름

```txt
1. 배열 정렬
2. left=0, right=0으로 시작
3. diff = arr[right] - arr[left]
4. diff < M 이면 right++
5. diff >= M 이면 answer 갱신 후 left++
6. left==right 이면 right++로 벌려서 left < right 유지
7. 포인터 범위 끝나면 answer 반환
```

---

## 🧩 코드 구현 (내 풀이)

```js
const fs = require("fs");
const input = fs.readFileSync(0, "utf-8").trim().split("\n");

function solution(M, arr) {
  arr.sort((a, b) => a - b);

  let left = 0;
  let right = 0;
  let answer = Infinity;

  while (right < arr.length && left < arr.length) {
    const diff = arr[right] - arr[left];

    if (diff < M) {
      right += 1;
    } else {
      if (diff < answer) answer = diff;
      left += 1;

      // 같은 원소 비교를 피하기 위해 포인터 겹침 해소
      if (left === right) right += 1;
    }
  }

  return answer;
}

const [N, M] = input[0].split(" ").map(Number);
const arr = input.slice(1, N + 1).map(Number);
console.log(solution(M, arr));
```

---

## 🧩 시행착오 및 수정 사항

### 1) 포인터 겹침(`left === right`) 처리 누락

문제:

* 같은 인덱스를 비교하면 `diff = 0`이 되어 의미 없는 비교가 반복될 수 있음

수정:

```js
if (left === right) right++;
```

의미:

* 항상 `left < right`를 유지하는 안전장치

### 2) 왜 `right`를 줄이지 않나?

핵심:

* 정렬 상태에서 `right`를 키우면 diff가 커지는 방향
* `diff < M`일 때는 오직 `right++`만이 조건 만족 방향으로 이동

즉, 이 문제는 단조성을 이용해 포인터를 한 방향으로만 전진시키는 것이 핵심이다.

### 3) 로컬 파일 입력 사용

기존:

```js
fs.readFileSync("../예제.txt")
```

문제:

* BOJ 제출 환경과 다름

수정:

```js
fs.readFileSync(0, "utf-8")
```

---

## 🧩 사용된 JS 개념

* `sort((a, b) => a - b)` : 숫자 오름차순 정렬
* 투 포인터 : 구간 조건 탐색
* 조건 기반 포인터 이동
* 표준 입력 파싱(`fs.readFileSync(0, "utf-8")`)

---

## 🧩 복잡도

* 시간복잡도 : `O(n log n)` (정렬) + `O(n)` (투 포인터)
* 공간복잡도 : `O(1)` (입력 배열 외 추가 메모리 기준)

---

## 🧠 사고 키워드

* 정렬 후 단조성 활용
* 차이 최소화
* 조건 이상/미만에 따른 포인터 분기
* 포인터 불변식 유지

---

## 🔍 트리거 문장

* "두 수의 차이가 M 이상인 최소값"
* "정렬 후 투 포인터로 조건 만족 구간 탐색"

---

## ⚠️ 오답 포인트

* 포인터 겹침 처리 누락
* `diff < M`에서 `left`를 움직여 역방향 탐색
* 정렬 없이 투 포인터를 적용하는 실수
