## 🧩 문제

* **문제 제목** : 투포인터 정렬 유형
* **문제 레벨** : 강의 문제집
* **문제 유형** : 정렬 + 투포인터 (양끝 포인터)
* **문제 제공** : Inflearn Kyo
* **문제 링크** : 강의 문제집 내 제공 문제
* **코드 파일** : [two-pointer-sort-kyo.js](../../algorithm-JS/1.%20Stack_Queue_Hash/two-pointer-sort-kyo.js)

---

## 🧩 문제 설명

사람들의 몸무게 배열 `weights`와 보트 제한 무게 `limit`가 주어진다.  
보트 한 대에는 최대 2명까지 탈 수 있고, 합이 `limit` 이하여야 한다.  
모든 사람을 옮기기 위해 필요한 최소 보트 수를 구한다.

---

## 🧩 문제 핵심 포인트

* 목표는 "보트 수 최소화"다.
* 가장 무거운 사람은 반드시 어떤 보트에는 타야 하므로, 매 단계에서 `right`를 줄이는 전략이 핵심이다.
* 가장 무거운 사람과 가장 가벼운 사람을 짝지을 수 있으면 같이 태우고, 아니면 무거운 사람만 태운다.
* 이를 위해 정렬 후 양끝 투포인터를 사용한다.

---

## 🧩 내 풀이 방식

* `weights`를 오름차순 정렬한다.
* `left`는 가장 가벼운 사람, `right`는 가장 무거운 사람을 가리킨다.
* `weights[left] + weights[right] <= limit`이면 함께 태우므로 `left++`.
* `right`는 항상 탑승 처리되므로 매번 `right--`.
* 루프 1회마다 보트 1대 사용(`truck++`), 종료 후 `truck` 반환.

---

## 🧩 코드 로직 정리

### 전체 흐름

```txt
1. 몸무게 배열 오름차순 정렬
2. left=0, right=n-1, truck=0
3. left <= right 동안 반복
4. light + heavy <= limit 이면 left++
5. right--, truck++
6. truck 반환
```

---

## 🧩 코드 구현 (내 풀이)

```js
function solution(weights, limit) {
  // 1) 몸무게를 오름차순 정렬
  // 2) 가장 가벼운 사람(left) + 가장 무거운 사람(right)을 함께 태울 수 있으면 left++
  // 3) 무거운 사람(right)은 항상 이번 보트에 탑승하므로 right--
  // 4) 보트 수(truck) 증가
  weights.sort((a, b) => a - b);

  let left = 0;
  let right = weights.length - 1;
  let truck = 0;

  while (left <= right) {
    if (weights[left] + weights[right] <= limit) {
      left += 1;
    }
    right -= 1;
    truck += 1;
  }

  return truck;
}
```

---

## 🧩 사용된 JS 개념

* `sort((a,b)=>a-b)` : 숫자 오름차순 정렬
* 투포인터(`left`, `right`) : 양끝에서 조건 검사
* `while` 반복 : 포인터 수렴까지 진행

---

## 🧩 복잡도

* 시간복잡도 : `O(n log n)` (정렬) + `O(n)` (투포인터)
* 공간복잡도 : `O(1)` (정렬 제외 포인터 변수)

---

## 🧠 사고 키워드

* 정렬 후 양끝 매칭
* 최솟값/최댓값 동시 고려
* 그리디 + 투포인터

---

## 🔍 트리거 문장

* "2명까지 태울 수 있다"
* "합이 limit 이하여야 한다"
* "최소 보트 수"

---

## ⚠️ 오답 포인트

* `left < right`로 두면 마지막 1명을 놓칠 수 있다. (`left <= right` 필요)
* `return truck`을 빼먹으면 `undefined`가 나온다.
* 정렬 없이 투포인터를 적용하면 매칭이 틀어진다.

