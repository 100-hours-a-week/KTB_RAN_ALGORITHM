### 🧩문제
- 문제 제목 : 택배 박스 정리 시스템
- 문제 유형 : 스택
- 문제 제공 : kyo 

- **boxes  |  result**
- [2, 2, 5, 5, 5, 1, 1] [2, 5, 1]
- [7, 7, 8, 8, 0, 0, 0] [7, 8, 0]


### 🧩코드 로직
- **내 풀이**
- 집합 Set을 이용하여 중복 제거
- 무엇보다 문제는 **연속으로 같은 값이 나올때만 중복 제거**
  - set은 안의 중복될 때 하나 제외하여 제거한다. 
  

```js
function solution(boxes) {
    let box = new Set(boxes);
    return [...box]
}
```

- **kyo 풀이**
- submit 배열에서 가장 위에 있는 값과 꺼낸값이 다를때만 넣는다. 
```js
function solution(boxes) {
    const submit = []; //결과

    for (const box of boxes){
        if(submit[submit.length-1]!==box){
            submit.push(box);
        }
    }
    return submit;
}
```

- Set : 전체 중복 제거
- Stack : 연속 중복 제거 