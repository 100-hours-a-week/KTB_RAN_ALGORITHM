## 🧩 문제

* **문제 제목** : 선물 배분하기
* **문제 레벨** : 강의 문제집
* **문제 유형** : 정렬 + 투포인터 (그리디 매칭)
* **문제 제공** : Inflearn Kyo
* **문제 링크** : 강의 문제집 내 제공 문제
* **코드 파일** : [gift-distribution-kyo.js](../../algorithm-JS/1.%20Stack_Queue_Hash/gift-distribution-kyo.js)

---

## 🧩 문제 설명

직원별 최소 기대 만족도 배열 `m`과 선물 가치 배열 `v`가 주어진다.  
직원 1명당 선물 1개만 줄 수 있을 때, 만족하는 직원 수의 최댓값을 구한다.  
직원은 선물 가치가 자신의 기대치 이상일 때 만족한다.

---

## 🧩 문제 핵심 포인트

* 작은 기대치 직원에게 작은 선물을 우선 매칭해야 전체 만족 인원을 극대화할 수 있다.
* 그래서 `m`, `v`를 모두 오름차순 정렬한다.
* `v[j] >= m[i]`면 해당 직원을 만족시킬 수 있으므로 둘 다 한 칸 이동한다.
* 부족한 선물(`v[j] < m[i]`)은 현재 직원을 절대 만족 못시키므로 선물 포인터만 이동한다.

---

## 🧩 내 풀이 방식

* 직원 기대치 배열 `m`, 선물 가치 배열 `v`를 오름차순 정렬한다.
* `i`(직원), `j`(선물) 포인터를 0에서 시작한다.
* 현재 선물이 현재 직원을 만족시키면 `satisfiedCount++`, `i++`, `j++`.
* 만족 못시키면 더 큰 선물을 보기 위해 `j++`.
* 한쪽 배열을 다 쓰면 종료하고 `satisfiedCount` 반환.

---

## 🧩 코드 로직 정리

### 전체 흐름

```txt
1. m, v 오름차순 정렬
2. i=0(직원), j=0(선물), count=0
3. i < m.length && j < v.length 동안 반복
4. v[j] >= m[i] 이면 매칭 성공: count++, i++, j++
5. 아니면 선물만 증가: j++
6. count 반환
```

---

## 🧩 코드 구현 (내 풀이)

```js
function solution(m, v) {
  // m: 직원별 최소 기대 만족도
  // v: 선물 가치
  m.sort((a, b) => a - b);
  v.sort((a, b) => a - b);

  let satisfiedCount = 0; // 만족한 직원 수
  let i = 0; // 직원 포인터
  let j = 0; // 선물 포인터

  while (i < m.length && j < v.length) {
    if (v[j] >= m[i]) {
      // 현재 직원을 만족시키는 가장 작은 선물 매칭
      satisfiedCount += 1;
      i += 1;
      j += 1;
    } else {
      // 선물 가치가 부족하면 더 큰 선물로 이동
      j += 1;
    }
  }

  return satisfiedCount;
}
```

---

## 🧩 사용된 JS 개념

* `sort((a,b)=>a-b)` : 숫자 오름차순 정렬
* 투포인터(`i`, `j`) : 직원/선물 동시 순회
* `while` 루프 : 포인터 끝까지 그리디 매칭

---

## 🧩 복잡도

* 시간복잡도 : `O(n log n + k log k)` (`n=m.length`, `k=v.length`)
* 공간복잡도 : `O(1)` (정렬 제외)

---

## 🧠 사고 키워드

* 정렬 후 그리디 매칭
* 투포인터
* 최소 만족 조건 충족

---

## 🔍 트리거 문장

* "각 직원은 최소 기대치가 있다"
* "선물 하나는 한 명에게만"
* "만족 인원 최대화"

---

## ⚠️ 오답 포인트

* 정렬 없이 매칭하면 최적해를 보장할 수 없다.
* 매칭 성공 시 `i`, `j` 둘 다 이동해야 한다.
* `return satisfiedCount`를 빼먹으면 `undefined`가 나온다.

