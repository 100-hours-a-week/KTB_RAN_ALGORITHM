## 🧩 문제

* **문제 제목** : 부분합
* **문제 레벨** : Gold 4
* **문제 유형** : 투 포인터 / 슬라이딩 윈도우
* **문제 제공** : BOJ
* **문제 링크** : https://www.acmicpc.net/problem/1806
* **코드 파일** : [partial-sum-1806.js](../../algorithm-JS/1.%20Stack_Queue_Hash/partial-sum-1806.js)

---

## 🧠 문제 키워드

* 사용 알고리즘 : 슬라이딩 윈도우
* 합이 `S` 이상인 최소 길이 구간
* 윈도우 확장(`right++`) / 축소(`left++`)
* 조건 만족 중에는 최대한 줄이기
* 초기화가 정답률을 좌우

---

## 🧩 문제 설명

양의 정수로 이루어진 수열에서, 연속된 부분합이 `S` 이상이 되는 구간 중
길이가 가장 짧은 구간의 길이를 구하는 문제다.
그런 구간이 없으면 `0`을 출력한다.

---

## 🧩 제한사항

* `1 ≤ N ≤ 100,000`
* `1 ≤ S ≤ 100,000,000`
* 수열 원소는 자연수

---

## 🧩 입출력 예

| 입력 | 출력 |
| --- | --- |
| 10 15 / 5 1 3 5 10 7 4 9 2 8 | 2 |

---

## 🧩 문제 핵심 포인트

* 모든 원소가 양수라서 `right`를 늘리면 합이 커지고, `left`를 늘리면 합이 작아진다.
* 이 단조성 때문에 투 포인터가 성립한다.
* 조건(`sum >= S`)을 만족하는 순간마다 왼쪽을 줄여 최소 길이를 갱신해야 한다.

---

## 🧩 내 풀이 방식

* `left=0`, `right=0`, `currentSum=0`으로 시작
* `right`를 증가시키며 현재 합을 키운다.
* `currentSum >= S`이면
  * 현재 길이로 답 갱신
  * `left`를 이동해 합을 줄이며 더 짧은 구간 탐색
* 끝까지 갱신이 없으면 `0` 반환

---

## 🧩 코드 로직 정리

### 전체 흐름

```txt
1. minLen=Infinity, left=0, right=0, sum=0
2. right를 한 칸씩 늘리며 sum에 더함
3. sum >= S 인 동안:
   - 길이 갱신
   - sum에서 arr[left]를 빼고 left++
4. right++
5. minLen이 갱신되지 않았으면 0, 아니면 minLen 반환
```

---

## 🧩 코드 구현 (내 풀이)

```js
const fs = require("fs");
const input = fs.readFileSync(0, "utf-8").trim().split("\n");

function solution(S, arr) {
  let minLen = Infinity;
  let left = 0;
  let right = 0;
  let currentSum = 0;

  while (right < arr.length) {
    currentSum += arr[right];

    while (currentSum >= S) {
      const len = right - left + 1;
      if (len < minLen) minLen = len;

      currentSum -= arr[left];
      left += 1;
    }

    right += 1;
  }

  return minLen === Infinity ? 0 : minLen;
}

const [N, S] = input[0].split(" ").map(Number);
const arr = input[1].split(" ").map(Number);

console.log(solution(S, arr));
```

---

## 🧩 시행착오 및 수정 사항

### 1) 슬라이딩 윈도우 초기화값

핵심 포인트:

```js
let left = 0;
let right = 0;
let currentSum = 0;
```

초기에 두 원소를 미리 더해서 시작하면, 길이 1 정답 케이스를 놓칠 수 있다.

반례:

* `arr = [5, 1, 1]`, `S = 5`
* 정답은 길이 1인데, 두 개를 미리 더하면 길이 2부터 시작해 오답 가능

### 2) 조건을 `sum == S`로만 처리한 실수

문제:

* 목표는 `sum >= S`인 최소 길이인데 `==`만 보면 케이스 누락

수정:

```js
while (currentSum >= S) { ... }
```

### 3) 윈도우 축소 타이밍

문제:

* 조건 만족 후에도 바로 축소하지 않으면 최소 길이를 놓친다.

수정:

* `sum >= S` 동안 `left`를 당겨 가능한 최소 길이까지 줄임

---

## 🧩 사용된 JS 개념

* 투 포인터(슬라이딩 윈도우)
* `Infinity` 초기값 사용
* 표준 입력 파싱 (`fs.readFileSync(0, "utf-8")`)
* `while` 중첩으로 확장/축소 분리

---

## 🧩 복잡도

* 시간복잡도 : `O(N)` (각 포인터가 최대 N번 이동)
* 공간복잡도 : `O(1)`

---

## 🧠 사고 키워드

* 양수 배열 단조성
* 조건 만족 시 축소
* 최소 길이 갱신
* 초기화값이 로직의 시작점

---

## 🔍 트리거 문장

* "연속된 부분합이 S 이상인 최소 길이"
* "조건을 만족하는 최소 구간 길이"

---

## ⚠️ 오답 포인트

* 초기 합을 두 원소로 시작하는 실수
* `>=`가 아닌 `==`로 조건 검사
* 조건 만족 후 left를 바로 줄이지 않는 실수
