## 🧩 문제

* **문제 제목** : 두 큐 합 같게 만들기
* **문제 레벨** : Level 2
* **문제 유형** : 스택/큐 (Queue), 투 포인터
* **문제 제공** : Programmers
* **문제 링크** : https://school.programmers.co.kr/learn/courses/30/lessons/118667
* **코드 파일** : [two-queue-sum-equal-118667.js](../../algorithm-JS/1.%20Stack_Queue_Hash/two-queue-sum-equal-118667.js)

---

## 💡 사고 키워드

* 연속 구간
* 부분 합 비교
* 합이 크면 줄이고, 작으면 늘리기
* 원형 배열 인덱싱
* 투 포인터

### 🔍 트리거 문장

* "조건을 만족할 때까지 한쪽에서 빼고 다른 쪽에 넣는다."
* "최소 연산으로 두 합을 같게 만든다."


### ⚠️ 오답 포인트

* 전체 합 홀수 여부를 먼저 체크하지 않으면 불필요한 연산을 하게 된다.
* 종료 조건(최대 이동 횟수)을 두지 않으면 무한 루프에 빠질 수 있다.
* 실제 큐 시뮬레이션으로만 풀면 시간초과 가능성이 커진다.





## 🧩 문제 설명

길이가 같은 두 큐 `queue1`, `queue2`가 있다.

한 번의 연산은 아래와 같다.

* 한 큐에서 맨 앞 원소를 꺼낸다.
* 다른 큐의 맨 뒤에 넣는다.

이 연산을 최소 몇 번 해야 두 큐의 원소 합을 같게 만들 수 있는지 구하는 문제다.
불가능하면 `-1`을 반환한다.

---

## 🧩 문제 핵심 포인트

* 두 큐 합을 같게 하려면 전체 합이 반드시 짝수여야 한다.
* 실제 큐 push/pop을 매번 수행하면 비효율적이다.
* `queue1 + queue2`를 하나의 원형 배열로 보고, `queue1` 구간을 슬라이딩 윈도우로 이동하면 된다.
* 최소 횟수는 윈도우의 왼쪽/오른쪽 포인터 이동 횟수와 동일하다.

---

## 🧩 내 풀이 방식

* `sum1`, `sum2`를 계산하고 `total`이 홀수면 바로 `-1`.
* 목표 합 `target = total / 2`.
* `arr = [...queue1, ...queue2]`를 만들고, 현재 `queue1` 구간을 `[left, right]`로 관리한다.
* `current`가 `target`보다 크면 왼쪽 원소를 제외(`left++`), 작으면 오른쪽 원소를 포함(`right++`)한다.
* 무한 루프 방지를 위해 최대 이동 횟수를 `3 * n`으로 제한한다.

---

## 🧩 코드 로직 정리

### 전체 흐름

```txt
1. sum1, sum2, total 계산
2. total이 홀수면 -1 반환
3. target = total / 2
4. left=0, right=n-1, current=sum1 로 시작
5. current와 target 비교
   - current > target: left 이동(원소 제거)
   - current < target: right 이동(원소 추가)
6. current === target 이면 count 반환
7. 제한 횟수 초과 시 -1 반환
```

---

## 🧩 코드 구현 (내 풀이)

```js
function solution(queue1, queue2) {
  const n = queue1.length;
  const sum1 = queue1.reduce((acc, cur) => acc + cur, 0);
  const sum2 = queue2.reduce((acc, cur) => acc + cur, 0);

  const total = sum1 + sum2;
  if (total % 2 !== 0) return -1;

  const target = total / 2;
  const arr = [...queue1, ...queue2];
  const m = arr.length;

  let left = 0;
  let right = n - 1;
  let current = sum1;

  const limit = n * 3;

  for (let count = 0; count <= limit; count++) {
    if (current === target) return count;

    if (current > target) {
      current -= arr[left % m];
      left += 1;
    } else {
      right += 1;
      current += arr[right % m];
    }
  }

  return -1;
}
```

---

## 🧩 사용된 JS 개념

* `reduce()` : 큐 합 계산
* 전개 연산자(`...`) : 두 큐를 하나의 배열로 결합
* 모듈로 연산(`%`) : 원형 배열 인덱스 처리
* 투 포인터 : 윈도우 구간 이동으로 합 조절

---

## 🧩 복잡도

* 시간복잡도 : `O(n)` (포인터가 최대 약 `3n`번 이동)
* 공간복잡도 : `O(n)` (`queue1 + queue2` 배열)

---
