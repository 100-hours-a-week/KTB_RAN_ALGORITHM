### 🧩문제
- 문제 제목 : 알파벳 폭발
- 문제 유형 : 스택
- 문제 제공 : kyo


### 🧩코드 로직
- **내 풀이**
- F f / f F 둘 다 사라져야되는 케이스
- f F가 오면 둘다 삭제 
  -> char(들어갈) 값이 stack(이미 들어간)의 가장 끝자리 값(소문자일 경우)의 대문자인지 확인
  -> 또는, stack 값이 대문자일 경우 소문자 인지 확인

```js
function isLowerCase(char){
    return char === char.toLowerCase();
}

function isUpperCase(char){
    return char === char.toUpperCase();
}

function solution(s) {
    let stack = [];

    for(let char of s) {
        if (stack.length === 0) {
            stack.push(char);
        } else {
            if (isLowerCase(stack[stack.length - 1]) && stack[stack.length - 1].toUpperCase() === char) {
                stack.pop();
            } else if (isUpperCase(stack[stack.length - 1]) && stack[stack.length - 1].toLowerCase() === char) {
                stack.pop();
            } else{
                stack.push(char);
            }
        }
    }

    return stack.join('');

}

```


- **kyo 풀이**

- `charCodeAt()`을 사용하였다.
- A= 65, a = 97
- 둘의 차이가 32이며 다른 알파벳도 동일 

```js
function solution(s) {
    let stack = [];

    for(let char of s) {
        const top = stack[stack.length - 1];
        if(top && Math.abs(top.charCodeAt() - char.charCodeAt()) === 32) {
            stack.pop();
        } else {
            stack.push(char);
        }
    }

    return stack.join('');

}

```
 
---
 
### 회고
해당 문제가 어렵다고 느꼈던 이유는 변수명을 직관적으로 지칭해주지 않아서 헷갈렸다.
특히, "stack[stack.length - 1]" 이렇게나 길 경우 변수명을 지정해줘서 문제풀이에 어떤 역할을 하는지
직관적으로 이해하기 쉽게 작성할 필요가 있다고 느꼈다.
예를들어 kyo 처럼, top이라고 작성해주어서 어떤 역할을 하는 변수인지 변수의 의미를 명확하게 활용하였다. 

코테를 풀때, 우선 가장 base가 될 코드를 작성하고 해당 코드에서 예외처리 해주는 식으로 진행하는 것이 좋은 것 같다.

나는 지금까지 항상 예외처리까지 바로 생각하려했기 때문에 고민하는 시간이 길었는데, 먼저 뼈가 될 코드를 먼저 작성하는 것이 좋을 것 같다.