## 🧩 문제

* **문제 제목** : 파일 합치기 3
* **문제 레벨** : Gold 4
* **문제 유형** : 힙/우선순위 큐 (Heap, Priority Queue), Greedy
* **문제 제공** : BOJ
* **문제 링크** : https://www.acmicpc.net/problem/13975
* **코드 파일** : [13975.js](../../algorithm-JS/Heap_PriorityQueue/13975.js)

---

## 🧠 문제 키워드

* 사용 알고리즘 : 최소 힙 + 그리디
* 가장 작은 두 파일 반복 병합
* 병합 비용 누적 최소화
* 테스트케이스 다중 처리
* 대용량 입력 대응

---

## 🧩 문제 설명

여러 파일을 하나로 합칠 때, 두 파일을 합치는 비용은 두 파일 크기의 합이다.
모든 파일을 하나로 만들 때 필요한 총 비용의 최솟값을 구하는 문제다.
테스트케이스가 여러 개 주어진다.

---

## 🧩 제한사항

* 테스트케이스 수 `T`
* 각 테스트케이스 파일 수 `K`
* 파일 크기는 양의 정수

(입력 크기가 커서 `O(K log K)` 이하가 필요)

---

## 🧩 입출력 예

| 입력 | 출력 |
| --- | --- |
| 2 / 4 / 40 30 30 50 / 15 / 1 21 3 4 5 35 5 4 3 5 98 21 14 17 32 | 300 / 826 |

---

## 🧩 문제 핵심 포인트

* 매 단계에서 가장 작은 두 파일을 먼저 합쳐야 최종 비용이 최소가 된다.
* 최소값 2개를 반복적으로 꺼내야 하므로 최소 힙이 적합하다.
* 정렬을 반복하면 비효율적이므로 힙 기반 접근이 필요하다.

---

## 🧩 내 풀이 방식

* 각 테스트케이스마다 파일 배열로 최소 힙을 구성한다.
* 힙에서 최소값 두 개를 꺼내 합치고, 비용에 더한 뒤 다시 힙에 삽입한다.
* 힙 크기가 1이 될 때까지 반복한 누적 비용을 정답으로 저장한다.
* 모든 테스트케이스 결과를 줄바꿈으로 출력한다.

---

## 🧩 코드 로직 정리

### 전체 흐름

```txt
1. T 읽기
2. 각 테스트케이스마다 K와 파일 크기 배열 읽기
3. 파일 배열로 최소 힙 build
4. 힙 크기가 1보다 큰 동안:
   - 최소 2개 추출
   - sum = first + second
   - total += sum
   - sum 재삽입
5. total을 정답 배열에 추가
6. 정답 배열 출력
```

---

## 🧩 코드 구현 (내 풀이)

```js
const fs = require("fs");
const input = fs.readFileSync(0, "utf-8").trim().split(/\s+/).map(Number);

class MinHeap {
  constructor() {
    this.heap = [];
  }

  size() {
    return this.heap.length;
  }

  build(arr) {
    this.heap = arr.slice();
    for (let i = Math.floor((this.heap.length - 2) / 2); i >= 0; i--) {
      this.heapifyDownFrom(i);
    }
  }

  insert(value) {
    const heap = this.heap;
    heap.push(value);
    this.heapifyUp();
  }

  extractMin() {
    const heap = this.heap;
    if (heap.length === 0) return null;
    if (heap.length === 1) return heap.pop();

    const min = heap[0];
    heap[0] = heap.pop();
    this.heapifyDown();
    return min;
  }

  heapifyUp() {
    const heap = this.heap;
    let currentIndex = heap.length - 1;

    while (currentIndex > 0) {
      const parentIndex = Math.floor((currentIndex - 1) / 2);
      if (heap[parentIndex] <= heap[currentIndex]) break;

      const tmp = heap[parentIndex];
      heap[parentIndex] = heap[currentIndex];
      heap[currentIndex] = tmp;
      currentIndex = parentIndex;
    }
  }

  heapifyDown() {
    this.heapifyDownFrom(0);
  }

  heapifyDownFrom(startIndex) {
    const heap = this.heap;
    const length = heap.length;
    let currentIndex = startIndex;

    while (true) {
      const leftChildIndex = currentIndex * 2 + 1;
      const rightChildIndex = currentIndex * 2 + 2;
      let minIndex = currentIndex;

      if (leftChildIndex < length && heap[leftChildIndex] < heap[minIndex]) {
        minIndex = leftChildIndex;
      }
      if (rightChildIndex < length && heap[rightChildIndex] < heap[minIndex]) {
        minIndex = rightChildIndex;
      }
      if (minIndex === currentIndex) break;

      const tmp = heap[minIndex];
      heap[minIndex] = heap[currentIndex];
      heap[currentIndex] = tmp;
      currentIndex = minIndex;
    }
  }
}

function solveCase(files) {
  if (files.length <= 1) return 0;

  const minHeap = new MinHeap();
  minHeap.build(files);

  let total = 0;

  while (minHeap.size() > 1) {
    const first = minHeap.extractMin();
    const second = minHeap.extractMin();
    const sum = first + second;
    total += sum;
    minHeap.insert(sum);
  }

  return total;
}

let idx = 0;
const t = input[idx++];
const answer = [];

for (let tc = 0; tc < t; tc++) {
  const k = input[idx++];
  const files = input.slice(idx, idx + k);
  idx += k;
  answer.push(solveCase(files));
}

console.log(answer.join("\n"));
```

---

## 🧩 시행착오 및 수정 사항

### 1) 로컬 파일 경로 입력 사용

기존:

```js
fs.readFileSync("../예제.txt")
```

문제:

* BOJ 채점 환경에서 동작하지 않음

수정:

```js
fs.readFileSync(0, "utf-8")
```

### 2) 테스트케이스 입력 파싱 실수 위험

문제:

* 줄 단위 처리만 가정하면 공백/개행 케이스에서 파싱 오류 가능

수정:

* `split(/\s+/)` 토큰 파싱 + 인덱스 포인터(`idx`) 방식으로 안정화

### 3) 힙 초기화 비용

문제:

* 파일을 하나씩 `insert`하면 초기 구성에 `O(K log K)`

수정:

* `build()`(bottom-up heapify)로 `O(K)` 초기화

---

## 🧩 사용된 JS 개념

* `split(/\s+/)` : 대용량 토큰 파싱
* `class` : 최소 힙 구현
* `for` + 인덱스 포인터 : 테스트케이스 순차 파싱
* 사용자 정의 힙 빌드(`build`) 및 추출/삽입

---

## 🧩 복잡도

* 시간복잡도 : 테스트케이스당 `O(K log K)`
* 공간복잡도 : 테스트케이스당 `O(K)`

---

## 🧠 사고 키워드

* 최소값 2개 반복 선택
* 병합 비용 최소화
* 그리디 + 힙
* 대용량 입력 파싱

---

## 🔍 트리거 문장

* "두 파일을 합치는 비용은 두 파일 크기의 합"
* "전체 합치는 비용을 최소화"

---

## ⚠️ 오답 포인트

* 입력 파싱을 줄 단위로 고정해 공백 케이스를 놓치는 문제
* 정렬 반복으로 인한 시간 초과
* 로컬 경로 입력 코드 그대로 제출하는 실수
