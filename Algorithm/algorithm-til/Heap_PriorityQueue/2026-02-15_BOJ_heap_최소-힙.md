## 🧩 문제

* **문제 제목** : 최소 힙
* **문제 레벨** : Silver 2
* **문제 유형** : 힙/우선순위 큐 (Heap, Priority Queue)
* **문제 제공** : BOJ
* **문제 링크** : https://www.acmicpc.net/problem/1927
* **코드 파일** : [1927.js](../../algorithm-JS/Heap_PriorityQueue/1927.js)

---

## 🧠 문제 키워드

* 사용 알고리즘 : 최소 힙(우선순위 큐)
* 삽입 + 최소값 추출 연산 반복
* `0` 명령 시 추출/출력
* 빈 힙 예외 처리
* 연산 수 `N`이 커서 `O(log n)` 연산 필요

---

## 🧩 문제 설명

처음 비어 있는 배열에서 아래 연산을 처리한다.

1. 자연수 `x` 입력: 배열에 `x` 삽입
2. `0` 입력: 배열에서 가장 작은 값을 출력하고 제거  
   단, 배열이 비어 있으면 `0` 출력

입력된 연산을 순서대로 수행하며, `0`이 입력된 횟수만큼 결과를 출력하는 문제다.

---

## 🧩 제한사항

* `1 ≤ N ≤ 100,000`
* `x`는 `0` 또는 자연수 (`2^31` 미만)

---

## 🧩 입출력 예

| 입력 | 출력 |
| --- | --- |
| 9 / 0 / 12345678 / 1 / 2 / 0 / 0 / 0 / 0 / 32 | 0 / 1 / 2 / 12345678 / 0 |

---

## 🧩 문제 핵심 포인트

* 필요한 연산은 "삽입"과 "최소값 추출"뿐이다.
* 최소 힙을 쓰면 두 연산 모두 `O(log n)`에 처리 가능하다.
* 추출 요청 시 힙이 비어 있으면 `0`을 출력해야 한다.

---

## 🧩 내 풀이 방식

* `MinHeap` 클래스를 직접 구현했다.
* `x > 0`이면 힙에 삽입한다.
* `x === 0`이면:
  * 힙이 비어 있으면 `0` 추가
  * 아니면 `extractMin()` 결과 추가
* 결과 배열을 마지막에 줄바꿈(`\n`)으로 출력한다.

---

## 🧩 코드 로직 정리

### 전체 흐름

```txt
1. MinHeap 준비
2. 명령 x를 순서대로 확인
3. x가 0이면:
   - 힙 비어있음 -> 0 출력 배열에 추가
   - 힙 비어있지 않음 -> extractMin() 결과 추가
4. x가 자연수면 insert(x)
5. 출력 배열을 줄바꿈으로 출력
```

---

## 🧩 코드 구현 (내 풀이)

```js
const fs = require("fs");
const input = fs.readFileSync(0, "utf-8").trim().split("\n");

class MinHeap {
  constructor() {
    this.heap = [];
  }

  size() {
    return this.heap.length;
  }

  insert(value) {
    const heap = this.heap;
    heap.push(value);
    this.heapifyUp();
  }

  extractMin() {
    const heap = this.heap;
    if (heap.length === 0) return null;
    if (heap.length === 1) return heap.pop();

    const min = heap[0];
    heap[0] = heap.pop();
    this.heapifyDown();
    return min;
  }

  heapifyUp() {
    const heap = this.heap;
    let currentIndex = heap.length - 1;

    while (currentIndex > 0) {
      const parentIndex = Math.floor((currentIndex - 1) / 2);
      if (heap[parentIndex] <= heap[currentIndex]) break;

      const swap = heap[parentIndex];
      heap[parentIndex] = heap[currentIndex];
      heap[currentIndex] = swap;
      currentIndex = parentIndex;
    }
  }

  heapifyDown() {
    const heap = this.heap;
    let currentIndex = 0;

    while (true) {
      const leftChildIndex = currentIndex * 2 + 1;
      const rightChildIndex = currentIndex * 2 + 2;
      let minIndex = currentIndex;

      if (
        leftChildIndex < heap.length &&
        heap[leftChildIndex] < heap[minIndex]
      ) {
        minIndex = leftChildIndex;
      }
      if (
        rightChildIndex < heap.length &&
        heap[rightChildIndex] < heap[minIndex]
      ) {
        minIndex = rightChildIndex;
      }

      if (minIndex === currentIndex) break;

      const swap = heap[minIndex];
      heap[minIndex] = heap[currentIndex];
      heap[currentIndex] = swap;
      currentIndex = minIndex;
    }
  }
}

function solution(n, arr) {
  const minHeap = new MinHeap();
  const answer = [];

  for (const x of arr) {
    if (x === 0) {
      if (minHeap.size() === 0) answer.push(0);
      else answer.push(minHeap.extractMin());
    } else {
      minHeap.insert(x);
    }
  }

  return answer;
}

const n = Number(input[0]);
const arr = input.slice(1).map(Number);
console.log(solution(n, arr).join("\n"));
```

---

## 🧩 시행착오 및 수정 사항

### 1) 로컬 파일 경로 입력 사용

기존:

```js
fs.readFileSync("../예제.txt")
```

문제:

* BOJ 채점 환경에서는 표준 입력(`fd 0`)을 읽어야 함

수정:

```js
fs.readFileSync(0, "utf-8")
```

### 2) 불필요한 디버그 출력

기존:

```js
console.log(minHeap);
```

문제:

* 정답 출력 형식을 깨뜨림

수정:

* 디버그 출력 제거

### 3) 빈 힙 처리 분기 누락 위험

문제:

* `0` 명령 시 빈 힙인데 추출하면 `null` 처리 이슈 발생 가능

수정:

* `minHeap.size() === 0`일 때 `0` 출력하도록 명시

---

## 🧩 사용된 JS 개념

* `class` : 최소 힙 직접 구현
* `fs.readFileSync(0, "utf-8")` : BOJ 표준 입력
* `for...of` : 명령 순차 처리
* 배열(`answer`) 누적 후 `join("\n")` 출력

---

## 🧩 복잡도

* 시간복잡도 : `O(N log N)`
* 공간복잡도 : `O(N)`

---

## 🧠 사고 키워드

* 최소값 반복 추출
* 삽입/삭제 동시 처리
* 빈 컨테이너 예외 처리
* 출력 포맷 관리

---

## 🔍 트리거 문장

* "`0`이면 최솟값을 꺼내 출력한다."
* "삽입과 최솟값 제거를 반복한다."

---

## ⚠️ 오답 포인트

* `console.log` 디버그 출력이 남으면 출력 형식 오답
* `0` 명령 처리 시 빈 힙 예외 처리 누락
* 표준 입력 대신 로컬 파일 경로를 쓰면 채점 실패
