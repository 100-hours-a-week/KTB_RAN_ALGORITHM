## 🧩 문제

* **문제 제목** : 바이러스
* **문제 레벨** : Silver III
* **문제 유형** : 그래프 탐색, BFS/DFS, 연결 요소
* **문제 제공** : BOJ
* **문제 링크** : https://www.acmicpc.net/problem/2606
* **코드 파일** : [virus-2606.js](../../algorithm-JS/Search_Graph/virus-2606.js)

---

## 🧩 문제 설명

1번 컴퓨터가 웜 바이러스에 감염되었을 때,  
네트워크 상에서 1번과 연결된(직접/간접) 다른 컴퓨터가 총 몇 대 감염되는지 구하는 문제다.

---

## 🧩 문제 핵심 포인트

* 문제의 본질은 "1번 정점에서 도달 가능한 정점 개수"를 세는 것이다.
* 무방향 그래프이므로 간선 `[u, v]`를 양쪽에 모두 추가해야 한다.
* BFS/DFS 어느 쪽으로 풀어도 되며, 방문 체크가 핵심이다.
* 시작점(1번)은 감염 수에서 제외하고, 새로 방문한 정점만 카운트한다.

---

## 🧩 내 풀이 방식

* 간선 입력을 인접 리스트로 변환했다.
* `bfs(1)`로 1번에서 출발해 연결된 컴퓨터를 순회했다.
* 방문하지 않은 이웃을 큐에 넣을 때마다 `count++` 했다.
* 최종 `count`를 출력했다.

---

## 🧩 코드 로직 정리

### 전체 흐름

```txt
1. N, M 및 간선 입력
2. 인접 리스트 graph 생성 (양방향)
3. bfs(1) 시작
4. 방문하지 않은 이웃을 큐에 넣으며 count 증가
5. bfs 종료 후 count 반환
```

---

## 🧩 코드 구현 (내 풀이)

```js
const fs = require("fs");
const input = fs.readFileSync(0, "utf-8").trim().split("\n");

function bfs(start, graph, visited) {
  const q = [start];
  let head = 0;
  let count = 0;

  visited.add(start);

  while (head < q.length) {
    const node = q[head++];

    for (const child of graph[node]) {
      if (!visited.has(child)) {
        visited.add(child);
        q.push(child);
        count += 1;
      }
    }
  }

  return count;
}

function solution(N, M, edge) {
  const graph = Array.from({ length: N + 1 }, () => []);

  for (const [u, v] of edge) {
    graph[u].push(v);
    graph[v].push(u);
  }

  const visited = new Set();
  return bfs(1, graph, visited);
}

const N = Number(input[0]);
const M = Number(input[1]);
const edge = input.slice(2, M + 2).map((line) => line.split(" ").map(Number));

console.log(solution(N, M, edge));
```

---

## 🧩 사용된 JS 개념

* `Array.from` : 인접 리스트 초기화
* `Set` : 방문 정점 체크
* 큐 배열 + `head` 포인터 : 효율적인 BFS 순회

---

## 🧩 복잡도

* 시간복잡도 : `O(N + M)`
* 공간복잡도 : `O(N + M)`

---

## 🧠 사고 키워드

* 도달 가능 정점 수
* BFS 탐색
* 무방향 그래프

---

## 🔍 트리거 문장

* "1번과 연결된 컴퓨터"
* "직접/간접 연결"
* "감염되는 컴퓨터 수"

---

## ⚠️ 오답 포인트

* 무방향 간선을 한쪽에만 추가하면 정답보다 작게 나온다.
* 시작 정점(1번)까지 count에 포함하면 1 크게 나온다.
* BFS에서 방문 체크 시점이 늦으면 중복 삽입으로 비효율이 커진다.
