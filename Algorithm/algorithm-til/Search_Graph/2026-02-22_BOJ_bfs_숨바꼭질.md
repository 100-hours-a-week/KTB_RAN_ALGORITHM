## 🧩 문제

- **문제 제목** : 숨바꼭질
- **문제 레벨** : Silver I
- **문제 유형** : BFS, 최단거리, 수직선 그래프
- **문제 제공** : BOJ
- **문제 링크** : https://www.acmicpc.net/problem/1697
- **코드 파일** : [hide-and-seek-1697.js](../../algorithm-JS/Search_Graph/hide-and-seek-1697.js)

---

## 🧩 문제 설명

수빈이는 현재 위치 `N`에 있고, 동생은 `K`에 있다.  
1초에 할 수 있는 이동은 3가지다.

- `x - 1`
- `x + 1`
- `x * 2`

수빈이가 동생을 만나는 **가장 빠른 시간(최소 초)**을 구하면 된다.

---

## 🧩 문제 핵심 포인트

- 한 줄 핵심: **격자 BFS의 4방향 대신, 이 문제는 `[-1, +1, *2]` 세 방향으로 탐색하는 BFS다.**
- 이 문제는 격자가 아니라, `0 ~ 100000` 숫자 칸 위에서 움직이는 문제다.
- 숫자 하나를 노드로 보면, 이동 연산(`-1`, `+1`, `*2`)이 간선 역할을 한다.
- 모든 이동 비용이 1초로 같으니, 최단거리는 BFS가 맞다.
- `visited`로 중복 방문을 막고, `dist`에 걸린 시간을 저장한다.

### 📌 유형 감지 메모

- `최소 시간`, `가장 빠르게`, `최단거리` 같은 표현이 보이면 BFS를 먼저 의심한다.
- 격자가 아니어도 된다. 이 문제는 "수직선 그래프 BFS"다.

---

## 🧩 내 풀이 방식

- `visited[0..100000]`, `dist[0..100000]` 1차원 배열을 만든다.
- 시작점 `N`을 큐에 넣고 BFS 시작.
- 매 단계에서 다음 위치 3개(`x-1`, `x+1`, `x*2`)를 만든다.
- 범위를 벗어나지 않고 아직 방문하지 않았으면:
  - 방문 처리
  - `dist[next] = dist[x] + 1`
  - 큐 삽입
- `next === K`를 만나면 즉시 시간 반환.

---

## 🧩 코드 로직 정리

### 전체 흐름

```txt
1. N === K면 0 반환
2. visited, dist 배열 준비 (크기 100001)
3. queue = [N], visited[N] = true
4. queue가 빌 때까지 반복
5. 다음 위치 3개를 검사: x-1, x+1, x*2
6. 범위 안 + 미방문이면 방문 처리, 거리 갱신, 큐 삽입
7. next가 K면 dist[next] 즉시 반환
```

### 동작 과정 (5 -> 17 느낌)

```txt
시작: 5
1초 후보: 4, 6, 10
2초 후보: (4에서), (6에서), (10에서)로 다시 퍼짐
...
레벨(시간) 단위로 퍼지다가 17을 처음 만나는 순간이 정답
```

핵심은 BFS가 거리 0 -> 1 -> 2 -> 3 순서대로 퍼진다는 점이다.

---

## 🧩 코드 구현 (내 풀이)

```js
const fs = require("fs"); // /dev/stdin
const input = fs.readFileSync("../예제.txt").toString().trim().split("\n");

function solution(N, K) {
  if (N === K) return 0;

  //2차원이 아니라 1차원 배열
  const visited = Array(100001).fill(false); //방문 노드 저장할 배열
  const dist = Array(100001).fill(0); //이동거리 저장할 곳

  //큐
  const queue = [N];
  visited[N] = true;

  while (queue.length) {
    //node는 이미 방문한 곳.
    //빼려는 이유: node의 인접한 친구를 확인하기 위해
    let node = queue.shift();

    for (let next of [node - 1, node + 1, node * 2]) {
      if (0 <= next && 100000 >= next && !visited[next]) {
        visited[next] = true;
        dist[next] = dist[node] + 1;

        if (K === next) return dist[next];

        queue.push(next);
      }
    }
  }

  return dist[K];
}

const [N, K] = input[0].split(" ").map(Number);

console.log(solution(N, K));
```

---

## 🧩 사용된 JS 개념

- 1차원 배열 초기화: `Array(100001).fill(...)`
- 배열 기반 큐: `push`, `shift`
- 반복 중 조기 반환: 목표 위치 도달 즉시 `return`

---

## 🧩 복잡도

- 시간복잡도 : `O(MAX)` (`MAX = 100000`)
- 공간복잡도 : `O(MAX)`

---

## 🧠 사고 키워드

- 수직선 그래프
- 가중치 없는 최단거리
- BFS 레벨 탐색

---

## 🔍 트리거 문장

- "가장 빠른 시간"
- "1초 후에 x-1, x+1, 2\*x 이동"
- "0 이상 100000 이하"

---

## ⚠️ 오답 포인트

- 범위 체크(`0 <= next <= 100000`)를 빼먹으면 런타임 오류가 난다.
- 방문 처리 전에 큐에 넣으면 같은 위치가 중복 삽입된다.
- 이 문제를 DFS로 풀면 최소 시간 보장이 어렵다.
- `N === K`인 경우를 바로 처리하지 않으면 불필요한 탐색이 생긴다.
