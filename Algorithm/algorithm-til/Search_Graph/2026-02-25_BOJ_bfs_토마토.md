## 🧩 문제

* **문제 제목** : 토마토
* **문제 레벨** : Gold V
* **문제 유형** : BFS, 격자 탐색, 멀티 소스 확산
* **문제 제공** : BOJ
* **문제 링크** : https://www.acmicpc.net/problem/7576
* **코드 파일** : [tomato-7576.js](../../algorithm-JS/Search_Graph/tomato-7576.js)

---

## 🧩 문제 설명

익은 토마토(`1`)는 하루가 지나면 상하좌우 인접한 안 익은 토마토(`0`)를 익게 만든다.  
비어 있는 칸은 `-1`이다.  
모든 토마토가 익는 최소 날짜를 구하고, 끝까지 익지 못하는 토마토가 있으면 `-1`을 출력한다.

---

## 🧩 문제 핵심 포인트

* 핵심은 익은 토마토들을 시작점으로 두고 BFS 확산을 수행해 날짜를 누적하는 것이다.
* 탐색이 끝난 뒤 `0`이 남아 있으면 도달 불가능 케이스이므로 `-1`을 반환한다.
* 현재 풀이에서는 먼저 전체를 순회해 시작점 후보를 모으고, 이후 확산 BFS를 수행하는 2단계 흐름으로 구현했다.

---

## 🧩 내 풀이 방식

* `queue`로 격자 전체를 훑으며 초기 익은 토마토 위치를 `searchQueue`에 모은다.
* `searchQueue`를 BFS 큐로 사용해 인접한 `0`을 익히고 값을 `현재값 + 1`로 기록한다.
* 마지막에 격자에 `0`이 남아 있는지 검사해 결과를 반환한다.

---

## 🧩 코드 로직 정리

### 전체 흐름

```txt
1. visited 배열 생성
2. queue로 격자 전체를 순회하며 값이 1인 좌표를 searchQueue에 수집
3. searchQueue 기준 BFS로 0인 칸을 익히며 날짜 누적
4. 탐색 종료 후 0이 남아 있으면 -1 반환
5. 모두 익었다면 (최댓값 - 1) 반환
```

---

## 🧩 코드 구현 (내 풀이)

```js
//토마토
const fs = require("fs"); // /dev/stdin
const input = fs.readFileSync("../예제.txt").toString().trim().split("\n");

//백준 제출 시
// const fs = require("fs");
// const input = fs.readFileSync(0, "utf-8").trim().split("\n");

//상하 좌우 다 탐색 해야할 것같음.
//방문은 어떻게 처리하지?

function solution(N, M, graph) {
  let visited = Array.from({ length: M }, () => Array(N).fill(false));

  //1찾기
  const queue = [];
  queue.push([0, 0]);
  const dx = [-1, 0, 1, 0];
  const dy = [0, 1, 0, -1];
  const searchQueue = [];
  let head = 0;

  while (head < queue.length) {
    let [curX, curY] = queue[head++];

    for (let i = 0; i < 4; i++) {
      let nextX = curX + dx[i];
      let nextY = curY + dy[i];

      if (
        nextX < M &&
        nextX >= 0 &&
        nextY < N &&
        nextY >= 0 &&
        visited[nextX][nextY] == false
      ) {
        queue.push([nextX, nextY]);
        visited[nextX][nextY] = true;
        if (graph[nextX][nextY] === 1) {
          searchQueue.push([nextX, nextY]);
        }
      }
    }
  }
  //   console.log(graph);
  //   console.log("searchQueue", searchQueue);

  //거리 기록
  let max = 0;
  head = 0;

  //1이 있는 자리부터 확인하기
  while (head < searchQueue.length) {
    let [curX, curY] = searchQueue[head++];

    for (let i = 0; i < 4; i++) {
      let nextX = curX + dx[i];
      let nextY = curY + dy[i];

      if (
        nextX < M &&
        nextX >= 0 &&
        nextY < N &&
        nextY >= 0 &&
        graph[nextX][nextY] == 0 //토마토가 존재함.
      ) {
        // console.log(nextX, nextY);
        searchQueue.push([nextX, nextY]); //추적함.
        visited[nextX][nextY] = true; //방문
        graph[nextX][nextY] = graph[curX][curY] + 1;
        // console.log(dist);
        max = graph[nextX][nextY];
      }
    }
  }
  //끝났을때 마지막 계산이
  //   console.log(graph);

  // 0 남았는지 검사
  for (let i = 0; i < M; i++) {
    for (let j = 0; j < N; j++) {
      if (graph[i][j] === 0) return -1;
    }
  }

  return max == 0 ? 0 : max - 1;
}

const [N, M] = input[0].split(" ").map(Number);
const arr = input.slice(1).map((i) => i.split(" ").map(Number));

console.log(solution(N, M, arr));
```

---

## 🧩 사용된 JS 개념

* `Array.from` : `visited` 2차원 배열 생성
* `queue + head 인덱스` : `shift` 없이 BFS 큐 순회
* `map(Number)` : 입력 문자열 숫자 변환

---

## 🧩 복잡도

* 시간복잡도 : `O(M*N)` (격자 순회 + BFS)
* 공간복잡도 : `O(M*N)` (`visited`, 큐)

---

## 🧠 사고 키워드

* 격자 확산 BFS
* 멀티 소스 시작점
* 남은 0 검증

---

## 🔍 트리거 문장

* "익은 토마토가 주변을 익힌다"
* "최소 일수"
* "모든 토마토가 익지 못할 수도 있다"

---

## ⚠️ 오답 포인트

* 마지막에 `0` 잔여 검사를 안 하면 불가능 케이스를 놓친다.
* 시작점(익은 토마토) 수집 로직이 누락되면 확산 BFS가 틀어진다.
* 날짜 출력에서 `max - 1` 보정 타이밍을 잘못 적용하면 정답이 어긋난다.
